[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --cov=anvyl_grpc_server
    --cov=anvyl_sdk
    --cov=database
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-fail-under=80